<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<features xmlns="http://karaf.apache.org/xmlns/features/v1.2.0" name="${project.artifactId}-${project.version}-feature">
	<feature name="${project.artifactId}" version="${project.version}" description="${project.name}">
		<bundle>mvn:${project.groupId}/${project.artifactId}/${project.version}</bundle>
	</feature>
</features>