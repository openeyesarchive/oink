<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:jee="http://www.springframework.org/schema/jee" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:rabbit="http://www.springframework.org/schema/rabbit"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit-1.0.xsd
	http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
	http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee.xsd">

	<!-- Root Context: defines shared resources visible to all other web components -->
	<!-- <context:property-placeholder location="file:${oink.config}" order="-1" 
		ignore-unresolvable="true" ignore-resource-not-found="true" /> -->
	
	<context:property-placeholder location="${oink.openeyes.properties}"
		order="1" ignore-unresolvable="true" ignore-resource-not-found="true" />
	<context:property-placeholder location="file:environment.properties" order="2" ignore-unresolvable="true" ignore-resource-not-found="true" />
	<context:property-placeholder order="3"
		location="classpath:/default.properties" ignore-unresolvable="true" />

	<rabbit:connection-factory id="connectionFactory" 
		host="${rabbit.server}" username="${rabbit.user}" password="${rabbit.password}"
		port="${rabbit.port}" virtual-host="${rabbit.vhost}" />
		
	<bean id="outOinkService" class="uk.org.openeyes.oink.messaging.OutboundOinkService">
		<constructor-arg name="rabbitConnection" ref="connectionFactory" />
		<constructor-arg name="replyQueueName" value="${rabbit.reply.queue}" />
		<constructor-arg name="exchangeName" value="${rabbit.exchange}" />
	</bean>
	
	<context:annotation-config />

</beans>