<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:p="http://www.springframework.org/schema/p"
	   xmlns:rabbit="http://www.springframework.org/schema/rabbit"
	   xmlns="http://www.springframework.org/schema/beans"
	   xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
	                       http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit-1.2.xsd
	                       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd">
	
	<context:property-placeholder location="classpath:rabbitmq.properties"/>

	<import resource="classpath:/oink-context-messaging-rabbitmq.xml" />

	<context:component-scan base-package="uk.org.openeyes.handlers">
		<context:exclude-filter expression="org.springframework.stereotype.Controller" type="annotation" />
	</context:component-scan>
	
    <bean id="inListener" class="uk.org.openeyes.infrastructure.messaging.impl.RabbitMQMessageHandler">
    </bean>
    
	<rabbit:listener-container id="inListenerContainer" connection-factory="connectionFactory" requeue-rejected="false"
		concurrency="${rabbitmq.in.consumers}">
		<rabbit:listener queue-names="${rabbitmq.in.name}" ref="inListener" />
	</rabbit:listener-container>

	<rabbit:connection-factory id="connectionFactory" host="${rabbitmq.host}" port="${rabbitmq.port}" connection-factory="nativeConnectionFactory" />

	<bean id="nativeConnectionFactory" class="com.rabbitmq.client.ConnectionFactory">
		<property name="host" value="${rabbitmq.host}" />
		<property name="requestedHeartbeat" value="${rabbitmq.requestedHeartbeat}" />
	</bean>

	<rabbit:admin connection-factory="connectionFactory" />
 	
</beans>