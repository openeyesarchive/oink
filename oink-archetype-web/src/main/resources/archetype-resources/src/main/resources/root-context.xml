<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:rabbit="http://www.springframework.org/schema/rabbit"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit-1.0.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- Properties need to be scanned in each context regardless of hierarchy -->
	<context:property-placeholder order="2"
		location="classpath:/default.properties" ignore-unresolvable="true" />

	<!-- Root Context: defines shared resources visible to all other web components -->
	<rabbit:connection-factory id="connectionFactory"
		host="${rabbit.server}" username="${rabbit.user}" password="${rabbit.password}"
		port="${rabbit.port}" virtual-host="${rabbit.vhost}" />

	<bean id="outOinkService" class="uk.org.openeyes.oink.messaging.OutboundOinkService">
		<constructor-arg name="rabbitConnection" ref="connectionFactory" />
		<constructor-arg name="replyQueueName"
			value="${rabbit.reply.queue.name}" />
		<constructor-arg name="exchangeName" value="${oink.exchange.name}" />
	</bean>

	<context:annotation-config />

</beans>
